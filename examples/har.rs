// use har::from_path;

#[tokio::main]
async fn main() {
    // match har::from_path("har_data/hard_data.har") {
    //     Ok(spec) => println!("spec: {:?}", spec),
    //     Err(err) => println!("error: {}", err),
    // }
}
