//! Add a product listing from a csv file
use std::error::Error;
use std::io;
use std::process;

use amazon_isr::helium10::FlattenedEntry;

// use polars::prelude::*;

fn main() {
    // let data = include_str!("../../listing_data/floating_wall_shelves/floating_wall_shelves.csv");

    // let mut rdr = csv::Reader::from_reader(data.as_bytes());

    // for result in rdr.deserialize() {
    //     // Notice that we need to provide a type hint for automatic
    //     // deserialization.
    //     let record: FlattenedEntry = result.unwrap();
    //     println!("{:?}", record);
    // }
}
